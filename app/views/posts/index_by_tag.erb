<%= render partial: 'posts', locals: {posts: @posts} %>
<%= render partial: 'paginate_tagged_posts', locals: {posts: @posts, tag_slug: @tag_slug} %>

<% @posts.each do |post| %>
    <p><%= post.comment_posts.humanize_count %></p>
<% end %>