<%= render partial: 'meta', locals: {subtitle: 'Archiwum'} %>
<%= render partial: 'posts/nav', locals: {actived_item: 'archives'} %>
<%= render partial: 'header' %>

<ul>
<% @posts.each do |post| %>
    <li><%= link_to post.title, post %></li>
<% end %>
</ul>